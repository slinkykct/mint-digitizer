# Site landing page

This folder is the simple PWA/landing page for MazeInt.

- The app is designed to be installable as a PWA when hosted on a static site.
- The desktop native app is still the recommended path for Windows users.
- For GitHub release publishing, use the generated EXE on the release page.
